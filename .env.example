# Cloudflare R2 custom domain for video hosting.
# Using a custom domain instead of the r2.dev URL enables CDN caching,
# removes rate limits, and avoids bot protection overhead.
VITE_R2_PUBLIC_URL=https://videos.nevomirzai.com

# GitHub credentials (for fetching repos during build)
GITHUB_USERNAME=
GITHUB_TOKEN=
