# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Video files — hosted on Cloudflare R2, not in the repo
# To keep videos locally for dev, they are loaded as fallback when VITE_R2_PUBLIC_URL is not set.
# Run `git rm -r --cached` after adding these patterns to remove already-tracked videos.
*.mp4
*.webm
*.mov
*.m4v
*.ogg

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env